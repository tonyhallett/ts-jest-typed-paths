{
  "name": "jest-readmes-generator",
  "version": "1.0.0",
  "description": "generates jest readmes",
  "private": true,
  "scripts": {
    "build": "ts-node ../../scripts/build-package.ts",
    "generate-readmes": "npm run build && ts-node generate-jest-readmes.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
