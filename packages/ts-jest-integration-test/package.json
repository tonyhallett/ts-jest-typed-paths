{
  "name": "ts-jest-integration-test",
  "version": "1.0.0",
  "description": "integration test",
  "private": true,
  "scripts": {
    "build": "ts-node ../../scripts/build-package.ts",
    "pretest": "npm run build",
    "test": "jest --no-cache",
    "preversions-test": "npm run build",
    "versions-test": "ts-node ./scripts/ts-version-test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-node": "^10.9.2",
    "ts-jest-typed-module-name": "*"
  }
}
